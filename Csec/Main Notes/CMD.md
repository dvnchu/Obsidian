[[Win]]
set to check path
systeminfo 
driverquery
help
cls clears

#### Network
ipconfig

ping target you send a specific ICMP packet and listen for a response if a response is received you can reach the target

tracert trace route traces the network route traversed to reach the target

nslookup looks up a host or domain and return its ip address

netstat current network connections and listening ports



#### Directories
cd  w/o parameter show where you are

dir show children dirs
dir /a show s hidden
/s files in current direc and all subdirec
tree  shows a tree
mkdir to create
rmdir to remove

##### Files

type to view text files and more for longer texts

copy to copy files

move is similar but jsut move

you can delete with del or erase

you can use wildcard * to refer to multiple files

- `chkdsk`: checks the file system and disk volumes for errors and bad sectors.
- `driverquery`: displays a list of installed device drivers.
- `sfc /scannow`: scans system files for corruption and repairs them if possible.